# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Local Streamlit state (per-machine cache; not part of source).
# Real secrets live in the Streamlit Cloud UI; locally we read from
# ~/.config/juno/claude-code/ files (see compat.py).
.streamlit/secrets.toml

# State files live under ~/.claude/scheduled-tasks/ — never inside the repo,
# but listed defensively in case anything escapes during dev.
*.bak
state.json
week_*.json
lunch_cover_*.json
scheduled_*.json
